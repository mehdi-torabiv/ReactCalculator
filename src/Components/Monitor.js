import React from 'react';
import './Monitor.css';
const Monitor = (props) => {
    return (
        <div className="Monitor">
            {props.input}
        </div>
    );
}

export default Monitor;
